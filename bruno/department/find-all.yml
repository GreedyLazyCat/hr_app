info:
  name: find-all
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/department?page=2"
  params:
    - name: name
      value: Финансовый
      type: query
      disabled: true
    - name: page
      value: "2"
      type: query
    - name: itemsPerPage
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
