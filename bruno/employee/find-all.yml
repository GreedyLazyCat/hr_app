info:
  name: find-all
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/employee"
  params:
    - name: fullName
      value: Иван
      type: query
      disabled: true
    - name: page
      value: "3"
      type: query
      disabled: true
    - name: itemsPerPage
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
