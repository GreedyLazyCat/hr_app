info:
  name: update
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/employee/:id"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "isFired": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
